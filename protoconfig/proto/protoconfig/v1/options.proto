syntax = "proto3";
package protoconfig.v1;

// Reserved: FieldOptions.env annotation used to exist here (field 1).
// It was removed because path-derived env keys are the single source of
// truth — per-field overrides invited schema drift. Reintroduce only with
// a clear migration story.
